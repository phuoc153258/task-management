import { ENV } from '../../config';

export const LEAVE_REQUEST_ROUTER = {
    index: `${ENV.apiUrl}/api/leave-request`,
    create: `${ENV.apiUrl}/api/leave-request`,
    show: `${ENV.apiUrl}/api/leave-request/`,
};
